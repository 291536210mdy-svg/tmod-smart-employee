# Award Review Business Line

The award review line wraps the existing `review_batch.py` engine. It keeps the scoring, ranking, fixed-priority rules, Excel export, internal pack, and QA logic in that script, while exposing it through the platform run interface.

